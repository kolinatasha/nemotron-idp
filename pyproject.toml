[project]
name = "nemotron-idp"
version = "0.1.0"
description = "Intelligent Document Processing (IDP) with Nemotron RAG"
requires-python = ">=3.10"

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"
